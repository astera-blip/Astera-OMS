rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Day 1 baseline: deny all access until public/private paths are designed and tested.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
